info:
  name: :slug
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/v1/guestwalls/:slug"
  params:
    - name: slug
      value: zoom
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
