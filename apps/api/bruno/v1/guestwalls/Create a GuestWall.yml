info:
  name: Create a GuestWall
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/v1/guestwalls/"
  body:
    type: json
    data: |-
      {
        "slug": "legal",
        "title": "Zoom's Amazing GuestWall",
        "description": "TheOldZoom's Amazing GuestWall",
        "requireApproval": false
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
