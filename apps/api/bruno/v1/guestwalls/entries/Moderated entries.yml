info:
  name: Moderated entries
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/v1/guestwalls/:slug/entries/moderation"
  params:
    - name: slug
      value: zoom
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
