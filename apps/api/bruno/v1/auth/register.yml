info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "displayName": "Zoom",
        "username": "theoldzoom",
        "email": "theoldzoom@proton.me",
        "password": "CoolSecretPassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
