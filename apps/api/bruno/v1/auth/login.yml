info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "theoldzoom@proton.me",
        "password": "CoolSecretPassword"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
